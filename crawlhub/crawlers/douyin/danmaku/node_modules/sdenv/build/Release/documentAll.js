/**
 * Pure JS mock for the native documentAll addon.
 * The real addon creates an "undetectable" object (typeof === 'undefined'),
 * which is impossible in pure JS. This mock creates a callable object with
 * the same properties, which should work for signature generation.
 */
function getDocumentAll(props) {
    const obj = function() { return null; };
    if (props && typeof props === 'object') {
        for (const [key, value] of Object.entries(props)) {
            obj[key] = value;
        }
    }
    return obj;
}

module.exports = { getDocumentAll };
