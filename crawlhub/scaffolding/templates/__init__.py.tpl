"""{{platform_display}} crawler plugin package.

Auto-discovered by ``crawlhub.core.registry.discover_platforms`` via
``plugin.yaml`` (next to this file). Do NOT import this package directly
from daemon / app code — go through the registry.
"""
