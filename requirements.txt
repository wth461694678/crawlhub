# CrawlHub - Unified Crawler Platform
# Single source of truth: pyproject.toml [project.dependencies]
# This file is a convenience mirror for `pip install -r requirements.txt`.
# Keep in sync with pyproject.toml when adding/removing deps.

# --- Core Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
click>=8.1
httpx>=0.27.0
pyyaml>=6.0

# --- Scheduler ---
apscheduler>=3.10

# --- Data / Export ---
openpyxl>=3.1
duckdb>=1.0.0

# --- WebSocket ---
websockets>=12.0

# --- Browser Automation (cookie refresh / login / BBA) ---
playwright>=1.40.0
# patchright = undetected fork of playwright; used by daemon BBA browser path
# (crawlhub.core.browser.playwright_runtime + douyin/kuaishou live_protocol).
# With channel="chrome" + --headless=new to bypass Douyin HeadlessChrome detection.
patchright>=1.58.0

# --- HTML Parsing (Steam service) ---
beautifulsoup4>=4.12.0

# --- HTTP Library (Steam service, failure detection) ---
requests>=2.31.0

# --- Date Utilities (cron template parsing) ---
python-dateutil>=2.8.2

# --- MCP Server (crawlhub_command tool) ---
mcp[cli]>=1.0.0

# --- TLS Fingerprint (Kuaishou anti-detection) ---
curl_cffi>=0.7.0

# --- Cryptography (Kuaishou protocol encryption) ---
gmssl>=3.2
pycryptodome>=3.19

# --- QR Code (Kuaishou cookie login) ---
qrcode>=7.4

# --- Process Monitoring (daemon health endpoints) ---
psutil>=5.9
